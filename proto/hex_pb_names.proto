syntax = "proto3";

message Names {
  // All packages in the repository
  repeated Package packages = 1;
}

message Package {
  // Package name
  string name = 1;
  // If set, the name of the package repository
  string repository = 2;
}
